# `hdeps`

`hdeps` helps manage Haskell package overrides for `nixpkgs`.
See <./example/> for an example.

## Acknowledgements

`hdeps` was written at [UpTrust](https://uptrusthq.com/).
If you're fed up with the current state of social media and want to help make something that's better for everyone,
as a user or a builder, then check them out.
